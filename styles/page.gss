@import "base.gss";
@import "typo.gss";
@import "viewport.gss";

.page {
    width: == [page-width];
    height: == ::window[height];
    height: == ::[intrinsic-height];
    center-x: == ::window[center-x];
    background: hotpink;
}